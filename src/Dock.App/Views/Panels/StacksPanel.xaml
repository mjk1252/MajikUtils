<UserControl x:Class="Dock.App.Views.Panels.StacksPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             AllowDrop="True"
             Drop="OnDrop"
             Background="Transparent">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid>
            <TextBlock Text="STACKS" Style="{StaticResource SectionHeaderText}"/>
            <TextBlock Text="Add folder..." Foreground="#99FFFFFF" FontSize="10"
                       HorizontalAlignment="Right" Cursor="Hand"
                       MouseLeftButtonUp="OnAddFolderClick"/>
        </Grid>

        <TextBlock Grid.Row="1" Style="{StaticResource EmptyStateText}"
                   Text="Each stack gets its own taskbar button -- click it to fan the folder open. Right-click the button and choose Pin to taskbar to keep it there."/>

        <TextBlock Grid.Row="2" Style="{StaticResource EmptyStateText}" Margin="4,8,4,4"
                   Text="No stacks yet -- use Add folder, or drag a folder onto this panel."
                   Visibility="{Binding Stacks.Count, Converter={StaticResource CountToVisibility}, ConverterParameter=invert}"/>

        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto" Margin="0,8,0,0">
            <ItemsControl ItemsSource="{Binding Stacks}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ListRow}" Cursor="Arrow"
                                MouseRightButtonUp="OnStackRightClick">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Image Source="{Binding IconPng, Converter={StaticResource PngToImage}}"
                                       Width="24" Height="24" Margin="0,0,10,0"
                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" Foreground="White"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding Path}" Foreground="#66FFFFFF" FontSize="10"
                                               TextTrimming="CharacterEllipsis"/>
                                </StackPanel>
                                <TextBlock Grid.Column="2" Text="Remove" Foreground="#99FFFFFF" FontSize="10"
                                           VerticalAlignment="Center" Cursor="Hand" Margin="8,0,4,0"
                                           MouseLeftButtonUp="OnRemoveClick"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
