<UserControl x:Class="Dock.App.Views.Panels.LauncherPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="OnLoaded">
    <!-- No margin of its own: SectionHost supplies the one gutter, shared by every panel. -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Rounded to match every other surface here, and watermarked the same way the island's
             own inputs are: a TextBlock behind the box, hidden the moment there is real text. -->
        <Border Height="36" Background="#20FFFFFF" CornerRadius="8">
            <Grid>
                <TextBlock Text="Search apps..." Foreground="#55FFFFFF" FontSize="14"
                           Margin="10,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False"
                           Visibility="{Binding Text.Length, ElementName=SearchBox,
                               Converter={StaticResource CountToVisibility}, ConverterParameter=invert}"/>

                <TextBox x:Name="SearchBox"
                         FontSize="14" Padding="10,6"
                         Background="Transparent" Foreground="White" BorderThickness="0"
                         VerticalContentAlignment="Center"
                         CaretBrush="White"
                         TextChanged="OnSearchChanged"/>
            </Grid>
        </Border>

        <TextBlock Grid.Row="1" Text="INSTALLED" Style="{StaticResource SectionHeaderText}" Margin="4,12,0,4"/>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding LauncherResults}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ListRow}" MouseLeftButtonUp="OnItemClick">
                            <StackPanel Orientation="Horizontal">
                                <Image Source="{Binding IconPng, Converter={StaticResource PngToImage}}"
                                       Width="22" Height="22" Margin="0,0,10,0"
                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                                <TextBlock Text="{Binding Name}" Foreground="White" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Visibility="{Binding WingetResults.Count, Converter={StaticResource CountToVisibility}}">
            <TextBlock Text="GET FROM WINGET" Style="{StaticResource SectionHeaderText}" Margin="4,14,0,4"/>
            <ScrollViewer MaxHeight="200" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding WingetResults}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="6,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{Binding Name}" Foreground="White"
                                           VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                <Button Grid.Column="1" Content="Install" Command="{Binding InstallCommand}"
                                        Style="{StaticResource ChipButton}"
                                        VerticalAlignment="Center" Margin="8,0,0,0"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>

        <TextBlock Grid.Row="4" Text="Searching winget..." Foreground="#77FFFFFF" FontSize="11"
                   Margin="4,10,0,0"
                   Visibility="{Binding IsWingetSearching, Converter={StaticResource BoolToVisibility}}"/>
    </Grid>
</UserControl>
