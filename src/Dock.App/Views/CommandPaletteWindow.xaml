<Window x:Class="Dock.App.Views.CommandPaletteWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MajikUtils"
        Width="560" SizeToContent="Height"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="True"
        Topmost="True"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        Deactivated="OnDeactivated"
        PreviewKeyDown="OnPreviewKeyDown">
    <Window.Resources>
        <FontFamily x:Key="GlyphFont">Segoe Fluent Icons, Segoe MDL2 Assets</FontFamily>

        <Style x:Key="PaletteInput" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource PanelBackgroundBrush}"
            BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1"
            CornerRadius="14">
        <Border.Effect>
            <DropShadowEffect BlurRadius="28" ShadowDepth="4" Opacity="0.5" Color="Black"/>
        </Border.Effect>

        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="&#xE721;" FontFamily="{StaticResource GlyphFont}"
                           FontSize="16" Foreground="#88FFFFFF" VerticalAlignment="Center"
                           Margin="2,0,12,0"/>

                <Grid Grid.Column="1">
                    <TextBlock Text="Search apps, stacks, recent files, clipboard..."
                               Foreground="#55FFFFFF" FontSize="18" IsHitTestVisible="False"
                               Visibility="{Binding Query.Length, Converter={StaticResource CountToVisibility}, ConverterParameter=invert}"/>

                    <TextBox x:Name="QueryInput" Style="{StaticResource PaletteInput}"
                             Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}"/>
                </Grid>
            </Grid>

            <Border Grid.Row="1" Height="1" Background="#22FFFFFF" Margin="0,14,0,0"
                    Visibility="{Binding Results.Count, Converter={StaticResource CountToVisibility}}"/>

            <ListBox x:Name="ResultsList" Grid.Row="2" Margin="0,10,0,0" MaxHeight="360"
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ItemsSource="{Binding Results}"
                     Visibility="{Binding Results.Count, Converter={StaticResource CountToVisibility}}">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="8"/>
                        <Setter Property="Margin" Value="0,1"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="Plate" Background="Transparent" CornerRadius="8"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Plate" Property="Background" Value="#18FFFFFF"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="Plate" Property="Background" Value="#2EFFFFFF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid MouseLeftButtonUp="OnResultClicked">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Width="28" Height="28" CornerRadius="6" Margin="0,0,10,0"
                                    Background="#22FFFFFF"
                                    Visibility="{Binding IconPng, Converter={StaticResource NullToVisibility}, ConverterParameter=invert}">
                                <Image Source="{Binding IconPng, Converter={StaticResource PngToImage}}"
                                       RenderOptions.BitmapScalingMode="HighQuality"/>
                            </Border>

                            <TextBlock Grid.Column="1" Text="{Binding Title}" Foreground="White"
                                       FontSize="13" VerticalAlignment="Center"
                                       TextTrimming="CharacterEllipsis"/>

                            <TextBlock Grid.Column="2" Text="{Binding Category}" Foreground="#77FFFFFF"
                                       FontSize="10" VerticalAlignment="Center" Margin="12,0,2,0"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Border>
</Window>
